library(mrgsolve)
library(dplyr)
library(tidyr)
# Example PK model - modify parameters according to your model
mod <- mcode("pkmodel", {
param(CL = 1, V = 10, KA = 1)
pkmodel(cmt = "DEPOT CENTRAL", depot = TRUE)
})
# Example PK model - modify parameters according to your model
mod <- mcode("pkmodel", {
param(CL = 1, V = 10, KA = 1)
pkmodel(cmt = 1, depot = TRUE)
})
mod <- mread("model/Linezolid_PKPD_model")
mod <- mread("model/Linezolid_PKPD_model")
